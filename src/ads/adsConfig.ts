/**
 * Рекламные блоки РСЯ (Yandex Mobile Ads).
 *
 * Сейчас подключены ДЕМО-блоки Яндекса — они подходят только для проверки
 * интеграции и не приносят доход. Перед публикацией:
 *   1. Зарегистрируйтесь на https://partner.yandex.ru/ и добавьте приложение;
 *   2. Создайте блоки: «Баннер» (sticky) и «Interstitial»;
 *   3. Замените adUnitId ниже на свои (вида R-M-1234567-1).
 */
export const ADS = {
  enabled: true,
  // Sticky-баннер внизу списка проектов и экрана сборки.
  bannerUnitId: 'R-M-19738635-1',
  // Полноэкранная реклама — только после успешной сборки (естественная пауза),
  // с ограничением частоты, чтобы не мешать работе (и не нарушать правила стора).
  interstitialUnitId: 'R-M-19738635-2',
  interstitialMinIntervalMs: 5 * 60 * 1000, // не чаще одного раза в 5 минут
};
